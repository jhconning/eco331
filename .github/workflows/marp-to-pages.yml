name: marp-to-pages
concurrency: marp-to-pages

on:
  push:
    branches: [ main ]
  pull_request:
    types:
      - opened
      - reopened
      - synchronize
      - closed
jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write
    steps:

      - name: Checkout code
        uses: actions/checkout@v3
        with:
          fetch-depth: 0

      - name: Ensure build dir exists
        run: mkdir -p docs/_build/html/slides

      - name: Copy images directory (if exists)
        run: |
          if [[ -d img ]]; then cp -R img docs/_build/html/img; fi
          if [[ -d slides/attachments ]]; then mkdir -p docs/_build/html/slides && cp -R slides/attachments docs/_build/html/slides/attachments; fi

      - name: Update Table of Contents in slideslist.md
        run: |
          deploy_date=$(TZ='America/New_York' date '+%Y-%m-%d %H:%M')
          echo -e "\n*Last Deployed: $deploy_date EST*\n" > toc.tmp
          echo -e "## Deployed Presentations\n" >> toc.tmp
          if [[ -d slides ]]; then
            find slides -type f -name "*.md" | sort | while read -r file; do
              if grep -q "^deploy:[[:space:]]*true" "$file"; then
                filename=$(basename "$file")
                title=$(grep -m 1 "^description:[[:space:]]*" "$file" | sed 's/^description:[[:space:]]*//' | sed 's/^"//;s/"$//' | sed "s/^'//;s/'$//" | tr -d '\r')
                if [ -z "$title" ]; then
                  title=$(grep -m 1 "^# " "$file" | sed 's/^# //' | tr -d '\r' || echo "${filename%.md}")
                fi
                edit_date=$(TZ='America/New_York' git log -1 --format="%cd" --date=format:"%Y-%m-%d %H:%M" -- "$file")
                if [ -z "$edit_date" ]; then edit_date="Unknown"; fi
                echo "- [$title](https://jhconning.github.io/eco331/slides/${filename%.md}.html) ([PDF](https://jhconning.github.io/eco331/slides/${filename%.md}.pdf)) *(Last edit: $edit_date)*" >> toc.tmp
              fi
            done
          fi
          awk '/<!-- TOC_START -->/{p=1; print; system("cat toc.tmp"); next} /<!-- TOC_END -->/{p=0} !p' docs/slideslist.md > docs/slideslist.new
          mv docs/slideslist.new docs/slideslist.md

      - name: Commit and Push slideslist
        run: |
          git config --global user.name 'github-actions[bot]'
          git config --global user.email 'github-actions[bot]@users.noreply.github.com'
          git add docs/slideslist.md
          git diff --quiet && git diff --staged --quiet || (git commit -m "Update Table of Contents [skip ci]" && git push)

      - name: Setup Node
        uses: actions/setup-node@v3
        with:
          node-version: 18

      - name: Install MyST
        run: npm install -g mystmd

      - name: Build MyST Site
        run: |
          cd docs
          myst build --html

      - name: Check if slides folder exists
        id: slides-folder-exists
        run: bash -c "[[ -d slides ]] ; echo \"has_slides=\$?\" >> \"$GITHUB_OUTPUT\""

      - name: Filter slides for deploy true
        if: steps.slides-folder-exists.outputs.has_slides == '0'
        run: |
          find slides -type f -name "*.md" | while read -r file; do
            if ! grep -q "^deploy:[[:space:]]*true" "$file"; then
              echo "Removing $file because it lacks deploy: true"
              rm "$file"
            fi
          done

      - name: Marp Build (slides folder, if exists)
        if: steps.slides-folder-exists.outputs.has_slides == '0'
        uses: docker://marpteam/marp-cli:v3.0.2
        with:
          args: -I slides/ -o docs/_build/html/slides/
        env:
          MARP_USER: root:root

      - name: Marp Build PDFs (slides folder, if exists)
        if: steps.slides-folder-exists.outputs.has_slides == '0'
        uses: docker://marpteam/marp-cli:v3.0.2
        with:
          args: -I slides/ -o docs/_build/html/slides/ --pdf --allow-local-files
        env:
          MARP_USER: root:root

      - name: Deploy preview
        if: ${{ github.event_name == 'pull_request' }}
        uses: rossjrw/pr-preview-action@v1
        with:
          source-dir: ./docs/_build/html/
          preview-branch: gh-pages
          umbrella-dir: pr-preview

      - name: Add .nojekyll
        run: touch docs/_build/html/.nojekyll

      - name: Deploy production
        if: ${{ github.event_name == 'push' }}
        uses: JamesIves/github-pages-deploy-action@v4
        with:
          branch: gh-pages
          folder: ./docs/_build/html/
          clean-exclude: pr-preview/
