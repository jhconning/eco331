# Raw Texts Directory

This directory stores the immutable, raw text extractions from Zotero PDFs.

The AI agent will read from this directory to generate summaries in the `wiki/` folder, but it should NEVER modify the contents of these raw text files.
